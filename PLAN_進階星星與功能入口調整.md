# 進階星星與功能入口調整計畫

> 建立時間：2026-07-27 10:03（台北時間）
> 類型：介面與紀錄規則改善
> 狀態：已執行

---

## 一句話版

把「英雄徽章館」移到世界地圖上方的原日曆位置，把「冒險打卡日曆」移到每週戰報；同時讓進階模式的星星永遠顯示最佳 `3` 筆評級，並依 `S → A → B` 排列。

## 本次調整範圍

### 1. 交換英雄徽章館與冒險打卡日曆入口

世界地圖上方目前的「冒險打卡日曆」入口改為「英雄徽章館」：

- 圖示改為勳章圖示。
- 提示文字與無障礙說明改為「英雄徽章館」。
- 點擊後直接進入英雄徽章館。
- 從英雄徽章館按返回時，回到世界地圖。

每週戰報右上角目前的「英雄徽章館」入口改為「冒險打卡日曆」：

- 圖示改為日曆圖示。
- 提示文字與無障礙說明改為「冒險打卡日曆」。
- 點擊後進入完整的冒險打卡日曆。
- 從冒險打卡日曆按返回時，回到每週戰報。

為了避免日曆入口仍然不明顯，在每週戰報的「本日打卡」區塊加入「查看日曆」文字入口；它和右上角日曆圖示會前往同一頁。

### 2. 進階星星顯示最佳 3 筆

進階模式只把通關且達到 `B` 以上的成績列入星星紀錄，評級順序固定為：

`S > A > B`

星星由左至右顯示最佳到最差：

- `B、A、S` 顯示為 `S、A、B`。
- `S、B` 顯示為 `S、B、空白`。
- `S、S、A` 顯示為 `S、S、A`。

世界地圖、我的冒險旅程、進階課程頁等所有使用進階星星的地方，統一套用同一個排序結果。

### 3. 新成績只在更好時取代最差紀錄

未滿 `3` 筆時：

- 新取得的 `S`、`A` 或 `B` 直接加入紀錄。
- 加入後重新依 `S → A → B` 排列。

已滿 `3` 筆時：

- 找出目前最差的一筆。
- 新評級嚴格高於最差評級時，才取代最差紀錄。
- 新評級與最差評級相同或更低時，星星紀錄完全不變。
- `C`、`D`、`E`、未通關或失敗場次都不影響星星紀錄。

這裡的「更好」只比較星等，不比較同星等之間的分數：

- `S` 高於 `A` 與 `B`。
- `A` 高於 `B`。
- 同為 `A` 時，即使新分數比較高，也不需要替換星星，因為畫面只保存及顯示評級。
- 既有的「最高分」仍照原本規則獨立更新，不受星星是否替換影響。

每次取得 `B` 以上的實際通關次數仍可繼續累積；畫面上的星星最多只顯示最佳 `3` 筆，兩者不互相混用。

## 預計修改位置

主要修改 `src/App.jsx`：

1. 調整進階星等讀取、排序與新增判斷。
2. 維持舊資料的相容處理；舊玩家沒有完整星等陣列時，仍能依既有通關數與最佳評級顯示。
3. 調整世界地圖上方入口的圖示、說明與點擊行為。
4. 調整每週戰報右上角入口，並在「本日打卡」加入「查看日曆」。
5. 調整主畫面的頁面切換與返回位置。

本次預計不修改資料庫結構，也不需要批次搬移舊玩家資料。

## 驗收案例

### 星星規則

| 原本最佳紀錄 | 新評級 | 更新後顯示 | 預期 |
|---|---:|---|---|
| 無 | B | B、空、空 | 加入 |
| B | S | S、B、空 | 加入並重排 |
| S、B | A | S、A、B | 加入並重排 |
| S、A、B | B | S、A、B | 不更新 |
| S、A、B | A | S、A、A | A 取代 B |
| S、A、A | S | S、S、A | S 取代 A |
| S、S、S | S | S、S、S | 不更新 |
| S、A、B | C／D／E | S、A、B | 不更新 |

另外確認：

- 未滿 `3` 顆時空星仍正常顯示。
- 世界地圖、我的冒險旅程與進階課程頁的順序一致。
- 舊玩家紀錄不會因新版排序而遺失。
- 通關累積數、完成 `3` 次的判斷與最佳評級分布不受影響。

### 入口與返回動線

1. 世界地圖上方點勳章圖示，可以進入英雄徽章館。
2. 英雄徽章館按返回，回到世界地圖。
3. 每週戰報右上角點日曆圖示，可以進入冒險打卡日曆。
4. 「本日打卡」區塊點「查看日曆」，可以進入同一個日曆頁。
5. 冒險打卡日曆按返回，回到每週戰報。
6. 原有每週戰報、今日打卡、成就進度與冷知識獎勵內容維持正常。

## 執行後驗證

1. 執行正式建置，確認沒有編譯錯誤。
2. 用上述星星組合逐一驗證排序與替換結果。
3. 在瀏覽器中走完兩組入口與返回動線。
4. 檢查手機寬度下，世界地圖上方按鈕與「本日打卡」區塊沒有擠壓或換行異常。
5. 查看實際差異，確認只修改本計畫列出的範圍。

## 本次不包含

- 不重新設計英雄徽章館的內容。
- 不修改成就條件或冷知識抽卡規則。
- 不修改冒險打卡的達成條件。
- 不修改進階評級門檻。
- 不保存同一星等內的第 `1`、第 `2`、第 `3` 高分明細。
- 不變更「英雄徽章館」的現有名稱。

## 確認後的執行順序

1. 先修改並驗證最佳 `3` 筆星等的排序與替換規則。
2. 再交換兩個入口及修正返回動線。
3. 加入「查看日曆」文字入口。
4. 執行建置與手動動線驗證。
5. 回報實際修改內容、驗證結果及任何需要注意的地方。

## 執行結果

- 已完成最佳 `3` 星的 `S → A → B` 排序與嚴格替換規則。
- 已完成英雄徽章館與冒險打卡日曆入口交換。
- 已修正兩個頁面的返回位置。
- 已在「本日打卡」加入「查看日曆」文字入口。
- 正式建置通過。
- 進階星等函式案例通過 `9 / 9`。
- 本機應用程式可正常載入；由於測試瀏覽器停在 Google 登入畫面，未使用使用者登入狀態操作內頁。
